{
  "version": 2,
  "builds": [
    {
      "src": "./server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/stripe/webhook", "methods": ["POST"], "dest": "./api/stripe/webhook" },
    { "src": "/api/(.*)", "dest": "./server.js" }
  ]
}

